# NPM Configuration for nestjs-solana-kit
# This file ensures consistent package installation behavior

# Use exact versions from pnpm-lock.yaml
# Prevents accidental version changes during installations
engine-strict=true
